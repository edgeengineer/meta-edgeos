# Set global WiFi regulatory domain default for EdgeOS
# The brcmfmac driver requires a valid country code to be set via regulatory hint
# This sets the global default; udev rule sends the PHY-specific hint via iw reg set
#
# Default: US
# The /etc/regdomain file controls the actual country - update that for provisioning
options cfg80211 ieee80211_regdom=US
